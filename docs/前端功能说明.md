# 前端功能说明

## 📋 核心功能概览

X-plan Demo 前端页面包含以下核心功能模块：

---

## 🏠 首页 / Dashboard

### 功能描述
展示项目概况和实时状态，提供安全提示和使用说明。

### 页面元素

#### 1. 地址信息展示区
```
┌─────────────────────────────────────────────────┐
│  地址 A - 被保护地址                             │
│  地址: 0x9aC84...                                │
│  私钥: 0x7b9d9c8... (可复制)                    │
│  ⚠️ 安全警告: 此私钥用于演示，请勿在其他场景使用  │
└─────────────────────────────────────────────────┘
```

#### 2. 实时余额监控
```
┌─────────────────────────────────────────────────┐
│  地址 A 余额                                     │
│  XPD: 1,000.00 XPD                              │
│  POL: 0.5 POL                                   │
│  🔄 最后更新: 2026-02-07 14:30:25              │
└─────────────────────────────────────────────────┘
```

#### 3. Worker 状态
```
┌─────────────────────────────────────────────────┐
│  Worker 运行状态                                 │
│  模式: 常规模式 / 应急模式                       │
│  扫描频率: 1分钟/次 / 5秒/次                     │
│  状态: 运行中 / 已停止                          │
└─────────────────────────────────────────────────┘
```

#### 4. 快捷操作按钮
- **[查看代币合约]** - 跳转到 PolygonScan
- **[获取测试 POL]** - 跳转到水龙头网站
- **[查看文档]** - 跳转到项目文档

---

## 🧪 测试页面

### 功能描述
提供测试工具，模拟资产转移和 Worker 保护场景。

### 页面元素

#### 1. 开始测试按钮 ⭐ 新增功能

**位置**: 测试页面顶部，醒目位置

**按钮样式**:
```
┌───────────────────────────────────┐
│     🚀 开始测试                   │
└───────────────────────────────────┘
```

**功能说明**:

从安全钱包（地址 B）转移 1 XPD 到被保护钱包（地址 A）

**触发条件**:
- 地址 B 必须有足够的 XPD 余额
- 地址 B 必须有足够的 POL 作为 Gas 费

**执行流程**:
```
用户点击 [开始测试]
    ↓
前端验证:
  - 检查地址 B 的 XPD 余额
  - 检查地址 B 的 POL 余额
    ↓
显示确认对话框:
  "将从安全钱包转移 1 XPD 到被保护钱包
   是否继续？"
    ↓
用户确认
    ↓
调用后端 API:
  POST /api/test/transfer
  Body: {
    "amount": "1000000000",  // 1 XPD (精度 9)
    "from": "地址B",
    "to": "地址A"
  }
    ↓
后端执行:
  - 连接到区块链网络
  - 签名交易
  - 发送交易到网络
    ↓
前端显示:
  - 交易处理中... (加载动画)
  - 交易成功 ✅ 或 交易失败 ❌
  - 交易哈希: 0xabc...
```

**前端代码示例**:

```typescript
// frontend/src/components/TestButton.tsx
import { useState } from 'react';

export default function TestButton() {
  const [loading, setLoading] = useState(false);
  const [txHash, setTxHash] = useState<string>('');

  const handleStartTest = async () => {
    // 显示确认对话框
    const confirmed = window.confirm(
      '将从安全钱包转移 1 XPD 到被保护钱包\n' +
      '是否继续？'
    );
    if (!confirmed) return;

    setLoading(true);
    try {
      // 调用后端 API
      const response = await fetch('/api/test/transfer', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          amount: '1000000000',  // 1 XPD (精度 9)
        }),
      });

      const data = await response.json();

      if (data.success) {
        setTxHash(data.txHash);
        alert('✅ 转账成功！\n交易哈希: ' + data.txHash);
      } else {
        alert('❌ 转账失败: ' + data.error);
      }
    } catch (error) {
      alert('❌ 网络错误: ' + error);
    } finally {
      setLoading(false);
    }
  };

  return (
    <button
      onClick={handleStartTest}
      disabled={loading}
      className="test-button"
    >
      {loading ? '处理中...' : '🚀 开始测试'}
    </button>
  );
}
```

**API 接口**:

```typescript
// POST /api/test/transfer
// Body: { amount: string }
// Response: {
//   success: boolean,
//   txHash: string,
//   error?: string
// }
```

**注意事项**:
- ⚠️ 此操作会消耗 POL 作为 Gas 费
- ⚠️ 每次测试前请确保地址 B 有足够的 POL
- ✅ 测试后可通过 Worker 将 XPD 转回地址 B
- ✅ 建议每次测试 1 XPD

---

#### 2. 模拟攻击测试

**功能**: 使用地址 A 的私钥，模拟攻击者转移资产

**界面元素**:
```
┌─────────────────────────────────────────────────┐
│  模拟攻击测试                                    │
│                                                 │
│  导入私钥: [输入框] (预填充地址 A 的私钥)        │
│  目标地址: [输入框]                             │
│  转移数量: [输入框] XPD                         │
│                                                 │
│  [发起攻击] 按钮                                 │
└─────────────────────────────────────────────────┘
```

**执行流程**:
```
用户点击 [发起攻击]
    ↓
前端验证私钥格式
    ↓
显示安全警告:
  "⚠️ 即将使用地址 A 的私钥发起转账
   请确保使用测试环境"
    ↓
用户确认
    ↓
调用 Web3 库签名交易
    ↓
发送交易到网络
    ↓
对比 Worker 响应:
  - 应急模式关闭 → 转账成功
  - 应急模式开启 → Worker 抢先转移，转账失败
```

---

## 🛡️ 应急控制页面

### 功能描述
控制 Worker 的应急状态开关

### 页面元素

#### 应急状态切换按钮
```
┌───────────────────────────────────┐
│  当前状态: 常规模式                 │
│  🔄 1分钟/次                      │
│                                   │
│  [启动应急状态]                   │
└───────────────────────────────────┘

⬇️ 点击后 ⬇️

┌───────────────────────────────────┐
│  当前状态: 应急模式 ⚠️             │
│  ⚡ 5秒/次                        │
│                                   │
│  [关闭应急状态]                   │
└───────────────────────────────────┘
```

**功能说明**:

**启动应急状态**:
- 将 Worker 切换到应急模式
- 扫描频率从 1 分钟提升到 5 秒
- 检测到余额立即转移到安全地址 B

**关闭应急状态**:
- 切换回常规模式
- 扫描频率恢复到 1 分钟
- 仅监控，不执行转移

**API 接口**:

```typescript
// POST /api/emergency/start
// Response: { success: boolean, mode: 'emergency' }

// POST /api/emergency/stop
// Response: { success: boolean, mode: 'normal' }

// GET /api/status
// Response: {
//   mode: 'normal' | 'emergency',
//   scanInterval: number,
//   lastScanTime: string
// }
```

---

## 📊 监控日志页面

### 功能描述
显示 Worker 的所有操作日志

### 页面元素

#### 日志列表
```
┌─────────────────────────────────────────────────┐
│  操作日志                                        │
│  [全部] [监控] [转移] [错误] [筛选▼]            │
├─────────────────────────────────────────────────┤
│  2026-02-07 14:30:20 [监控] 扫描完成            │
│    地址A余额: 1,000 XPD                          │
├─────────────────────────────────────────────────┤
│  2026-02-07 14:30:15 [转移] 应急保护触发         │
│    转移 1,000 XPD → 地址B                        │
│    交易哈希: 0xabc123...                         │
├─────────────────────────────────────────────────┤
│  2026-02-07 14:30:10 [转移] POL 转移完成         │
│    转移 0.1 POL → 地址C                          │
├─────────────────────────────────────────────────┤
│  2026-02-07 14:30:05 [错误] 交易失败             │
│    错误: Gas fee insufficient                   │
└─────────────────────────────────────────────────┘
```

#### 功能按钮
- [刷新日志]
- [导出日志]
- [自动刷新开关]

---

## 🔐 安全提示组件

### 功能描述
在整个应用中显示安全提示

### 提示内容

**首页顶部**:
```
⚠️ 重要提示
1. 本项目仅用于 Polygon Amoy 测试网
2. 请勿在主网使用真实资产
3. 地址 A 的私钥为演示用途，请勿在其他场景使用
4. 测试前请从水龙头获取足够的 POL
```

**测试页面**:
```
⚠️ 测试须知
- 每次测试消耗约 0.01 POL 作为 Gas 费
- 建议保持地址 C 至少 1 POL 作为备用 Gas 费
- 测试后可将 XPD 转回地址 B
```

---

## 📡 WebSocket 实时更新

### 功能描述
通过 WebSocket 实时推送余额和日志更新

### 事件类型

```typescript
// 余额更新
{
  type: 'balance_update',
  data: {
    address: string,
    xpdBalance: string,
    polBalance: string,
    timestamp: string
  }
}

// 日志更新
{
  type: 'log_update',
  data: {
    timestamp: string,
    level: 'info' | 'warn' | 'error',
    category: 'monitor' | 'transfer' | 'error',
    message: string,
    txHash?: string
  }
}

// 应急状态变更
{
  type: 'emergency_status',
  data: {
    mode: 'normal' | 'emergency',
    scanInterval: number
  }
}
```

---

## 🎨 UI 组件设计

### 按钮样式

```css
/* 开始测试按钮 */
.test-button {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 12px 24px;
  border-radius: 8px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
}

.test-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
}

.test-button:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

/* 启动应急状态按钮 */
.emergency-start {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

/* 关闭应急状态按钮 */
.emergency-stop {
  background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}
```

### 卡片样式

```css
.info-card {
  background: white;
  border-radius: 12px;
  padding: 24px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  margin-bottom: 16px;
}

.balance-value {
  font-size: 32px;
  font-weight: bold;
  color: #667eea;
}
```

---

## 📝 前端技术栈

- **框架**: Next.js 14 (App Router)
- **组件库**: TDesign
- **Web3**: ethers.js v6
- **状态管理**: React Context API
- **样式**: Tailwind CSS
- **实时通信**: WebSocket / Server-Sent Events

---

最后更新: 2026-02-07
