# DemoSite 启动说明

## 📝 启动方法

### 方法 1: 使用 PowerShell（推荐）

以**管理员权限**打开 PowerShell，执行：

```powershell
cd d:\TOBEHOST\xplan2026\frontend\DemoSite
powershell -ExecutionPolicy Bypass -File server.ps1
```

### 方法 2: 使用 Python（如果已安装）

```powershell
cd d:\TOBEHOST\xplan2026\frontend\DemoSite
python -m http.server 3000
```

或

```powershell
cd d:\TOBEHOST\xplan2026\frontend\DemoSite
python3 -m http.server 3000
```

### 方法 3: 使用 Node.js（如果已安装）

```powershell
cd d:\TOBEHOST\xplan2026\frontend\DemoSite
npx http-server -p 3000
```

### 方法 4: 使用 Live Server（VSCode 扩展）

1. 安装 "Live Server" 扩展
2. 右键点击 `index.html`
3. 选择 "Open with Live Server"

### 方法 5: 直接在浏览器中打开

在浏览器中打开：
```
file:///d:/TOBEHOST/xplan2026/frontend/DemoSite/index.html
```

---

## 🌐 访问地址

启动成功后，在浏览器中访问：

```
http://localhost:3000
```

或

```
http://127.0.0.1:3000
```

---

## ⚠️ 注意事项

1. **权限问题**: 如果遇到"拒绝访问"错误，请以管理员权限运行 PowerShell
2. **端口占用**: 如果 3000 端口被占用，修改 `server.ps1` 中的端口号
3. **CORS 跨域**: 如果 API 请求失败，确保 Worker 已配置 CORS

---

## 🔧 API 配置

修改 `js/api.js` 中的 API 地址：

```javascript
const API_CONFIG = {
  main: 'https://tactics-1.xplan2026.workers.dev',
  backup: 'https://tactics-1.xplan2026.workers.dev'
}
```

---

## 📦 需要的文件

确保以下文件存在：

```
frontend/DemoSite/
├── index.html          # 主页面
├── css/
│   ├── variables.css   # CSS 变量
│   └── styles.css     # 样式文件
├── js/
│   ├── api.js         # API 调用
│   ├── app.js         # 主应用逻辑
│   ├── components.js  # UI 组件
│   └── utils.js       # 工具函数
└── server.ps1         # PowerShell 服务器脚本
```

---

## 🎯 功能测试

### 1. 基础功能
- [ ] 页面加载正常
- [ ] 显示被保护地址 A 的信息
- [ ] 显示 XPD 和 POL 余额
- [ ] Worker 健康状态显示

### 2. API 连接
- [ ] Worker 健康检查通过
- [ ] 钱包状态加载成功
- [ ] 应急状态查询正常

### 3. 测试功能
- [ ] "开始测试"按钮可用
- [ ] 点击后调用测试转账 API
- [ ] 显示交易结果

### 4. 应急模式
- [ ] 启用应急模式按钮
- [ ] 禁用应急模式按钮
- [ ] 应急状态实时更新

---

**最后更新**: 2026-02-07
