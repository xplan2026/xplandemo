# Nginx 配置文件 - X-plan 项目
# 站点 1: 官方网站 (Official Site)
# 站点 2: Demo 站点 (DemoSite)
# 端口: 6060

# 官方网站配置
server {
    listen 6060;
    server_name _;

    # 官方网站根目录
    root /var/www/xplan-official-site;
    index index.html;

    # 访问日志和错误日志
    access_log /var/log/nginx/xplan-official-access.log;
    error_log /var/log/nginx/xplan-official-error.log;

    # 主要配置
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# Demo 站点配置（可选，需要配置反向代理或不同的端口）
# 如果需要同时访问 Demo 站点，可以使用以下配置：
# server {
#     listen 6061;
#     server_name _;
#
#     root /var/www/xplan-demosite;
#     index index.html;
#
#     access_log /var/log/nginx/xplan-demo-access.log;
#     error_log /var/log/nginx/xplan-demo-error.log;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
