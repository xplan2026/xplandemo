## 上传文件
upload 命令是 PinMe 的核心功能。它可以把单个文件或整个目录上传到 IPFS 网络，并可选地绑定到 ENS 域名。

## 命令语法
```bash
pinme upload [path] [--domain <name>]
pinme upload [path] [-d <name>]
```
## 参数说明

- path（可选）：要上传的文件或目录路径

- -d, --domain <name>（可选）：上传后要绑定的 PinMe 子域名

### 上传类型
**1. 交互式上传**
不指定路径直接执行可进入交互模式：
```bash
pinme upload
```
PinMe 会引导你选择文件或目录。

**2. 上传单个文件**
```bash
# 上传指定文件
pinme upload ./index.html

# 上传并绑定域名
pinme upload ./index.html --domain my-portfolio

# 使用短参数
pinme upload ./index.html -d my-portfolio
3. 上传目录
# 上传整个目录
pinme upload ./my-website

# 上传目录并绑定域名
pinme upload ./dist --domain my-app

# 上传嵌套目录
pinme upload ./build/static --domain assets
```


### 支持的文件类型
PinMe 可以上传任何文件类型：

- 音乐（MP3、WAV 等）
- 图片（JPG、PNG、GIF 等）
- 网站（HTML、CSS、JS）
- 视频、文档、配置文件
以及更多...

大小限制
- 单文件：最大 200MB
- 总存储空间：1GB

## 上传示例
### 示例 1：部署静态网站
```bash
# 先构建项目
npm run build

# 上传构建输出
pinme upload ./dist --domain my-awesome-site
```
### 示例 2：上传文档站点
```bash
# 上传 docs 目录
pinme upload ./docs --domain my-project-docs
```
### 示例 3：分享单个文件
```bash
# 上传一个 PDF 供分享
pinme upload ./report.pdf
```
## 上传过程中发生了什么？
### 1. 文件处理
- PinMe 读取并校验你的文件
- 根据限制计算文件大小
- 生成适用于 IPFS 的目录结构

### 2. 上传到 IPFS
- 文件被添加到 IPFS & Filecoin
- 内容由加密哈希（CID）标识
- 为保证持久可用，文件会被 Pin（固定）

### 3. 域名绑定（若指定）
- 创建/更新 ENS 子域名记录
- 域名指向你的 IPFS 内容哈希
- 通常需要 1-2 分钟传播

### 4. 返回信息
上传成功后，你会看到类似输出：
```bash
uploading /xxx.md to ipfs...
✔ Upload completed [████████████████████] 100% (9s)

  ____                                              __           _ 
 / ___|   _   _    ___    ___    ___   ___   ___   / _|  _   _  | |
 \___ \  | | | |  / __|  / __|  / _ \ / __| / __| | |_  | | | | | |
  ___) | | |_| | | (__  | (__  |  __/ \__ \ \__ \ |  _| | |_| | | |
 |____/   \__,_|  \___|  \___|  \___| |___/ |___/ |_|    \__,_| |_|
                                                                   

URL:
https://pinme.eth.limo/#/preview/U2FsdGVkX1_04_Hgws2BtGn0v7vUomlmN6L...


🎉 upload successful, program exit
```
## 上传选项与参数
### 域名绑定
```bash
# 绑定到自定义子域名
pinme upload ./site --domain my-site

# 域名命名规则：
# - 3-63 个字符
# - 小写字母、数字、连字符
# - 不能以连字符开头或结尾
# - 在 PinMe 全局范围内唯一
```


### 详细输出
```bash
# 显示详细上传进度
pinme upload ./site --verbose
```
## 最佳实践
### 目录组织
```bash
my-project/
├── index.html          # 入口文件
├── assets/
│   ├── css/
│   ├── js/
│   └── images/
└── dist/              # 构建产物
```


### 相对路径
在 HTML/CSS/JS 中使用相对路径：
```html
<!-- 推荐 -->
<link rel="stylesheet" href="./styles/main.css">
<img src="./images/logo.png">

<!-- 避免绝对路径 -->
<link rel="stylesheet" href="/styles/main.css">
```
### 文件压缩
- 上传前压缩图片
- 压缩/最小化 CSS、JavaScript
- 移除无用文件与依赖